{
  "name": "@workspace-todo-app/frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "rayfin env --framework vite",
    "prebuild": "rayfin env --framework vite",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:fabric": "tsc -b && vite build"
  },
  "dependencies": {
    "@workspace-todo-app/shared": "*",
    "@workspace-todo-app/data": "*",
    "@workspace-todo-app/functions": "*"
  },
  "devDependencies": {
    "@workspace-todo-app/local-dev": "*",
    "typescript": "^5.8.3"
  }
}
